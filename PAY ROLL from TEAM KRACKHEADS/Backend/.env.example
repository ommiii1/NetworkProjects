# MySQL database (required)
DATABASE_URL=mysql+pymysql://user:password@localhost:3306/blockchain

# Optional: Hela blockchain (if using on-chain features)
# HELA_RPC_URL=https://testnet-rpc.helachain.com
# CONTRACT_ADDRESS=0x... (deployed CorePayroll)
# TAX_VAULT_ADDRESS=0x...

# Optional: Default tax rate (%)
TAX_RATE=10
