# Backend API URL
VITE_API_URL=http://localhost:5000/api

# Oracle Private Key for OffRamp Rate Signing
# Generate a new wallet and use its private key here
# IMPORTANT: Keep this secret! Do not commit the actual .env file
# This wallet address must match the ORACLE_SIGNER used in contract deployment

VITE_ORACLE_PRIVATE_KEY=your_oracle_private_key_here

# Example: Generate a new wallet in Node.js:
# const { ethers } = require('ethers');
# const wallet = ethers.Wallet.createRandom();
# console.log('Address:', wallet.address);
# console.log('Private Key:', wallet.privateKey);

# Admin wallet address for accessing system logs (lowercase)
# This should match the ADMIN_ADDRESS in backend/.env
VITE_ADMIN_ADDRESS=0x0000000000000000000000000000000000000000
