# Supabase (login)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Contract: NativePayStream on HeLa Testnet (native HEA streaming)
# Set exactly one. Run `npm run deploy:native` in paystream-contract to get an address.
NEXT_PUBLIC_PAYSTREAM_ADDRESS=0x...

# Optional: only if you also use HLUSD PayStream (ERC20) flow
# NEXT_PUBLIC_HLUSD_ADDRESS=0x...
