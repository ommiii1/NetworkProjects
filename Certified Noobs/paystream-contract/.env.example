HELA_RPC_URL=https://testnet-rpc.helachain.com
HELA_CHAIN_ID=666888

# Required for deploy:native. The wallet that deploys becomes the primary HR (owner).
# Use the private key of the wallet you want as main HR (e.g. from MetaMask: Account details → Export private key).
PRIVATE_KEY=your_private_key_here

# Optional: for deploy:native — tax vault (compliance). A % of each employee withdraw goes here.
# Leave both unset for no tax. Tax vault = any EOA or contract that accepts HEA.
# TAX_VAULT_ADDRESS=0x...   # address to receive tax (native HEA)
# TAX_BPS=1000             # basis points (1000 = 10%)

# Optional: use existing contracts instead of deploying
# HLUSD_ADDRESS=0x...
